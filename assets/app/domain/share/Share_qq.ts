import { Share } from "./Share";
import { ShareParams } from "./ShareMgr";

export class ShareQQ extends Share{
    shareText(data : ShareParams) {
        
    }

    shareImage(data : ShareParams) {
        
    }

    shareLink(data : ShareParams) {
        
    }
}